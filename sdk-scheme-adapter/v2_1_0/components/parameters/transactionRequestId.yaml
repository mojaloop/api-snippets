name: transactionRequestId
in: path
required: true
schema:
  $ref: ../schemas/CorrelationId.yaml
description: >-
  Identifier of the merchant request to pay to continue as returned in
  the response to a `POST /requestToPay` request.
