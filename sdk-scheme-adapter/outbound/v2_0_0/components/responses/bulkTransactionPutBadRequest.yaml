description: Malformed or missing required body, headers or parameters
content:
  application/json:
    schema:
      oneOf:
        - $ref: ../schemas/bulkTransactionAcceptPartyErrorResponse.yaml
        - $ref: ../schemas/bulkTransactionAcceptQuoteErrorResponse.yaml
