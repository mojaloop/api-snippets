title: AccountsIDPutResponse
type: object
description: The object sent in a `PUT /accounts/{ID}` request.
properties:
  accounts:
    type: array
    items:
      $ref: ./Account.yaml
required:
  - accounts
