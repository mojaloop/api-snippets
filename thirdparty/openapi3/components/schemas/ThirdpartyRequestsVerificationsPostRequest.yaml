title: ThirdpartyRequestsTransactionAuthenticationValue
type: object
description: The object sent in the POST /thirdpartyRequests/verifications request.
properties:
  verificationRequestId:
    allOf:
      - $ref: ./CorrelationId.yaml
  challenge: 
    type: string
    description: Base64 encoded bytes - The challenge generated by the DFSP.
  value: 
    type: object
    properties:
      authenticationInfo:
        $ref: ./AuthenticationInfo.yaml
      responseType:
        $ref: '../../../../fspiop/v1_1/openapi3/components/schemas/AuthorizationResponse.yaml'
  consentId:
    allOf:
      - $ref: ./CorrelationId.yaml
    description: |
      The id of the stored consent object that contains the credential with which to verify
      the signed challenge against.
required:
  - verificationRequestId
  - challenge
  - value
  - consentId